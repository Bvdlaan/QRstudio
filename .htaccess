DirectoryIndex index.html

RewriteEngine On

# www naar non-www
RewriteCond %{HTTP_HOST} ^www\.qrstudio\.nl$ [NC]
RewriteRule ^ https://qrstudio.nl%{REQUEST_URI} [R=301,L]

# HTTP naar HTTPS
RewriteCond %{HTTPS} !=on
RewriteRule ^ https://qrstudio.nl%{REQUEST_URI} [R=301,L]
